#spring:
##  profiles:
##    active: dev
#  jpa:
#    hibernate:
#      ddl-auto: update
##    show-sql: true 쿼리문 보이게 하기
#  config:
#    activate:
#      on-profile: dev
#  datasource:
#    url: ${DB_URL}
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: ${DB_USER}
#    password: ${DB_PASSWORD}

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true